name: CI
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Run Nuclei
      uses: projectdiscovery/nuclei-action@main
      with:
        target: ‘http://selluseller.com/’
        github-report: true
        github-token: ${{ secrets.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 }}
