name: CI
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Run Nuclei
      uses: projectdiscovery/nuclei-action@main
      with:
        target: ‘http://app.selluseller.com/’
        github-report: true
        github-token: ${{ secrets.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 }}
